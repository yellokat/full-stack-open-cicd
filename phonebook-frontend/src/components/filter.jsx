import React from 'react'

const Filter = ({ onChange }) => {
    return (
        <>
            <>filter shown with </>
            <input onChange={onChange} />
        </>
    )
}

export default Filter